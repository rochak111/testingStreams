CREATE TABLE Matches (
    id SERIAL PRIMARY KEY,
    title character varying(255) NOT NULL,
    description character varying(255) NOT NULL,
    embed_code url NOT NULL
    -- created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP NOT NULL,
    -- updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP NOT NULL
);

INSERT INTO Matches (id, title, description,embed_code) VALUES 
(41, 'A vs B', 'Bundesliga','https://github.com/geshan/nodejs-posgresql/blob/master/db/init.sql'), 
(42, 'C vs D', 'EL-Classico','https://github.com/geshan/nodejs-posgresql/blob/master/db/init.sql'), 
(43, 'E vs F', 'EPL','https://github.com/geshan/nodejs-posgresql/blob/master/db/init.sql');
